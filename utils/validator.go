package utils

import (
	"github.com/go-playground/validator/v10"
)

func CreditCardValidator(fl validator.FieldLevel) bool {
	// Add logic for validating credit card numbers (e.g., Luhn algorithm)
	return true
}
